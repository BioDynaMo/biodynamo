#!/bin/bash

# this script echos the path to the biodynamo environment script
# On linux using a snap packages to source the environment script using the
# folloginw command:
#   . $(biodynamo.getenv)
echo /snap/biodynamo/current/biodynamo/bin/biodynamo.env
