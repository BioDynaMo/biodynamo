/**
 * This definitions are only needed to generate more meaningful collaboration charts in Doxygen.
 * This header MUST NOT be included anywhere!
 */
namespace std {
template<class T> class shared_ptr { T *dummy; };
template<class T> class unique_ptr { T *dummy; };
template<class T> class vector { T dummy; };
template<class T> class list { T dummy; };
template<class T, class U, class V, class W> class unordered_map {
  T dummy;
  U dummy_1;
  V dummy_2;
  W dummy_3;
};
}  // namespace std
